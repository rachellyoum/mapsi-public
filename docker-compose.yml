services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: 26-stormforge-backend
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env
    depends_on:
      #- db
      #- redis
#  db:
#    image: postgres:16
#    environment:
#      POSTGRES_USER: app
#      POSTGRES_PASSWORD: app_pw
#      POSTGRES_DB: app_db
#    ports:
#      - "5432:5432"
#    volumes:
#      - pgdata:/var/lib/postgresql/data

#  redis:
#    image: redis:7
#    ports:
#      - "6379:6379"

volumes:
  pgdata:
